1) The JSON.json file contains all the data used to recreate the design, Also there is a screenshot of the resulting design.
1) All the develompent code is inside the mock/ directory
2) The required files to run the app is mock/app/ directory, mock/main.js and mock/index.html
